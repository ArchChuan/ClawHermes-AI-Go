apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: clawhermes-local-ingress
  namespace: clawhermes
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "60"
spec:
  ingressClassName: nginx
  rules:
    - host: clawhermes.local
      http:
        paths:
          - path: /api/
            pathType: Prefix
            backend:
              service:
                name: clawhermes-ai
                port:
                  number: 80
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend-service
                port:
                  number: 80
